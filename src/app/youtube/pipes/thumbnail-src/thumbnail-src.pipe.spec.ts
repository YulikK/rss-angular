import { ThumbnailSrcPipe } from './thumbnail-src.pipe';

describe('ThumbnailSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
