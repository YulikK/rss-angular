import { FreshColorDirective } from './fresh-color.directive';

describe('FreshColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshColorDirective();
    expect(directive).toBeTruthy();
  });
});
